<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BasicApp.Views.DomainView">
    <StackLayout Spacing="20" Padding="15">
        <Label Text="Nazwa:" FontSize="Medium" />
        <Entry Text="{Binding Name}" d:Text="Nazwa" FontSize="Small" />
        <Label Text="Link:" FontSize="Medium" />
        <Entry Text="{Binding Link}" d:Text="Link" FontSize="Small"/>
        <Label Text="Koszt:" FontSize="Medium" />
        <Entry Text="{Binding Cost}" d:Text="Koszt" FontSize="Small"/>
        <Label Text="Opis:" FontSize="Medium" />
        <Entry Text="{Binding Description}" d:Text="Opis" FontSize="Small"/>
    </StackLayout>
    </ContentView>